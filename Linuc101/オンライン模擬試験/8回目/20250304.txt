■apt-getコマンドの「update」と「upgrade」サブコマンドの説明
違い。
updeteはパッケージのデータベース最新化
upgradeは更新できるものだけアップグレード
・「update」はパッケージのデータベースを最新版に更新する
 ・「upgrade」は全パッケージのうち、既存パッケージの削除や新規パッケージの追加をせずに更新できるものについて、アップグレードする

■ 以下の「ls -l」の実行結果からわかること
-rw-rw-r-- 3
実体を共有するファイルを「3つ」削除するとファイルの実体も削除される
ハードリンクの数
ハードリンクとはつまりそのファイルの命。
ハードリンクが0になると実体も削除されるため。


■viエディタのコマンドモードで、カーソルをファイルの先頭へ移動するviコマンド
gg
■viエディタのコマンドモードにおいて、カーソルのある行を含めて5行を切り取り、バッファにコピーしたい
5dd

■「/home」ディレクトリの詳細情報のみを表示させたい。
ls -dl /home
-dはディレクトリの詳細情報
■現在バックグラウンドで動作しているジョブと一時停止中のジョブを表示するコマンド
jobs
bg.fgは切り替えるコマンドであり、表示するコマンドではない！

■「/etc/passwd」ファイルの各行の2番目の文字を取り出して表示したい。
cut -c 2 /etc/passwd
cutのcです。dは区切り文字です

■Linuxカーネルなど、システムを起動するために必須のファイルを格納するディレクトリ
/boot

■viエディタでコマンドモードから入力モードに移行する際、カーソルのある行の下に空白行を挿入し、その行で文字を入力したい
o

■シンボリックリンクの説明
・異なったファイルシステム間でも作成可能
・lsコマンドで確認するとパーミッションの先頭に「l」が表示される
・シンボリックリンクはファイルの実体ではないため、inode番号とは異なる
ハードリンクは実体
シンボリックリンクはリンクである

■sedの使い方
誤：「最初の行」に現れる文字列を置換します。
正：「各行の最初」に現れる文字列を置換します。


■「/etc/fstab」ファイルでマウントオプションとしてdefaultsを指定した場合、どのマウントオプションが指定されたことになるか。
async, auto, dev, exec, nouser, rw, suid
覚えられないので、後半で覚える。
nouser, rw, suid
nouserで書き込みができること。

■以下のコマンドを実行した。プロセスID「1234」に送られるシグナル
TERM(SIGTERM)
killコマンドでシグナルを指定しない場合にデフォルトで送信されるシグナル。

■ハードリンクとシンボリックリンクそれぞれの説明
・ハードリンク先のファイルを編集すると、元ファイルも変更される(実体だから)
・ハードリンクはコピーに比べてディスク容量を消費しない(実体だから)
・シンボリックリンクの元ファイルを削除しても、リンクは残る(リンクだから)

■virshコマンドのサブコマンドで、仮想マシンを正常に終了するもの
shutdown
virshにはstopなんてない。

■多数のコンテナを効率的に管理するために、最適なソフトウェア
Kubernetes

Dockerもできそうですが
1台向けである。多数向けがKubernetes