■ホスト名を設定するファイルの絶対パス
/etc/hostname

■crontabの設定ファイル
/var/spool/cron
ポイント：
1.etcではないvarである。
crontabの設定だけど、パスはcronである。

■at設定ファイル
空のallowは誰も許可してないということ
空のdenyは誰も制限してないということ

■atコマンドによって登録されたジョブを削除できる。
rでremoveで削除というイメージなので
at -rとatrmは覚えられる
dでdeleteで削除するというremoveと類似したイメージを持てるかどうか？
at -d
atだとdeleteもremoveもあるよ、そういうこと。

■ユーザーごとパスワードの設定確認できるコマンド
chage

■オープンソースについて
インターネット上に公開できる必要はない。
要求された時に公開できれば良い。

特定の国で禁止することはできない。
