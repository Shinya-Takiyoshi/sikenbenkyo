■/etc/passwdの第二フィールド
xが入っている場合はx参照
x以外の場合は暗号化パスワードとみなす。

■/etc/pam.d
異なるアルゴリズムで暗号化されたとしても
アルゴリズムに応じて対応するため、システム管理者は何もする必要がない。

■ユーザーのプライマリーグループを登録するには
/etc/passwd

■グループにユーザーを追加するコマンド
usermod
groupmod:グループ名の変更。

■ユーザーアカウントを削除せずにログインできないようにする手順
/etc/passwdの最初の:の後に*(アスタリスク)を入れる
