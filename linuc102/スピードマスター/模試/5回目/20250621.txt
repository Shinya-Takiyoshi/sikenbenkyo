■#!/bin/bashの意味
インタプリタ(機械翻訳)を/bin/bashに指定するということ。

■ルーティングテーブルの表示
ssにはルーティングテーブルを表示するコマンドはない。
ip routeでもip route showでも表示はされる。
ip route系のコマンドが両方選択肢にある場合に、迷うがどちらもYESで良さそう。

■ソースコードの公開義務があるのは
GPLとMozillaでした。
ということでソースコードの公開義務=GM