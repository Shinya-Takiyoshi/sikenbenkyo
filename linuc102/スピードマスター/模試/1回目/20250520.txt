■icmpメッセージに含まれるものは？
時間切れ
到達不能
icmpエコーのリプライ

■空のグループを追加するコマンド
groupadd

■/etc/anacrontabで実行時間帯を指定するパラメータ
START_HOURS_RANGE

■cron表記の日曜の毎時15分
日曜は0
毎時は*か0-23(覚える！)

■ロケール設定に含まれないものは？
タイムゾーン

■/etc/localtimeの説明
タイムゾーンが◯◯の場合、
◯◯ファイルへのシンボリックリンクかコピー
ポイント：内容は実ファイルを参照しているということ。

■pool.ntp.orgの説明
DNS名前解決により、IPアドレスはリクエストごとに決まる。

■カーネルのメッセージを記録するrsyslog.conf
warn指定だとerrも含みになる。
errの方がプライオリティが強いため、warnの中にerrが内包されている。
