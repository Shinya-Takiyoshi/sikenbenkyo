■日付の計算
・+,-は数字しか計算できない。
・「fx」は完全指定、暗黙的変換できない。
■10 ROWS FETCH FIRST 10
・10行分スキップし、11番目から10行目取得
■BETWEEN
・BETWEEN A AND B == 列名　>= A AND 列名　<= B
■表の設計
・第三正規形
　主キー以外
　・第二正規形
　　すべての主キー
　　・第一正規形
　　　主キーだけ
■デリミタ
・qは引数→直前の列名が代入される。
■NULLの立ち位置
・ORDER BY で、NULL値はデフォルトで最大値
・NULLS FIRSTはNULLが上にくる
   

