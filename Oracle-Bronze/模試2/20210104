■日付書式
・デフォルトは「DD-MON-RR」
■日付の計算
MONTHES_BETWEEN()
6末-7末 = 1(ヶ月)
■USING句が適している場合
①データ型が異なる共通列は自然結合でエラーになるため、USINGで結合列を指定
②共通列にNULL値列がある場合
■COUNT
COUNT(*) //NULLあり全行数
COUNT(列名) //NULLなし行数
■完全外部結合
結合+結合しない行数も取得する
■複数列問い合わせ
IN()
