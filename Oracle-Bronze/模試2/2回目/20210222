■置換変数
defineで定義したものは入力を求められない。
■正規形
第一、第二は主キーである。
第三は主キー以外
■列別名
SELECT句内で定義した列別名は式で使用できない
■NULLの振る舞い
ソート順はNULLは最大値となる。
　→ASCは一番下
 　DESCは一番上
  
■日付書式
・デフォルト「DD-MON-RR」
 月が数値の場合「02」→「FEB」に置き換えることはできない。
・西暦
　yyyyもrrrrも可
■数値書式
fm(埋め込みモード解除)　かつ　[9]指定の場合、[0]は表示できない。
Vの場合、V以降に続く数　* n乗の桁になる。
つまり、V99になると、 0が二つ後ろにつく。

■GROUP BY
・列別名の指定ができない。
・グループ関数の指定ができない。

■集合演算子
ORDER BYは最後のSELECT文で指定する必要がある。

■リスト形式
LISTAGG(列名,区切り文字)
(ORDER BYかっこ指定)

■>ANY
ANY(MAXのリスト)のため、MAXのリストの中で一番低いもののより高いもの

■ROLLBACK TO a
ロールバックされるが、トランザクションは継続する。

■PK
追加したい定義の重複データが無ければ追加ができる。

■テーブル定義
NUMBER 小数点を省略した場合、38桁の少数を設定できる。
CLOB 4Gバイトまで
VARCHAR2 4000バイト

■列削除
・制約削除が必要

■LONG型
GROUP/ORDER BY　指定できない。
