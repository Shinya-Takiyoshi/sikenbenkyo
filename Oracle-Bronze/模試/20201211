■USING
・表別名で就職できない。
■副問い合わせ/結合が必要かどうか
・他の行の列を参照する必要があるかどうか
■NATURAL JOIN
・表別名で就職できない。
■<ANY(リスト)
・最大の中より小さいもの(つまり２番目)
■<ALL(リスト)
・全ての値よりも小さいもの(一番小さい)
■INTERSECT
・共通する行だけ取得
■MINUS
・1の表から2の表を引いた結果
■updateの副問い合わせ
・取得した列のみ使用
■VALUES句の副問い合わせ
・列は1つまで
■単一行/複数行関数
・結果が単一になるか、複数行かを
　DBの状態を確認して判断する。
 複数行の場合で単一の比較をした場合、単一の結果でない場合はエラーになる。
■UNIQUE、PK
・自動的に索引が作成される。
■INTERVAL DAY TO SECOND
・日付、時、分、秒を管理できる。
■デフォルト
SYSDATE ○
__VAL　✖️
シーケンス　開始:1 増分:1
