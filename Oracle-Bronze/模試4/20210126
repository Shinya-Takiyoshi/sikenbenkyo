■汎用関数
・その関数が何を戻すかを確認する。
・引数のデータ型一致が必要
■SUMとCOUNT
SUM→カウントする
COUNT→NULLを除く全行数をカウントする
結果：カウントする場合はSUM
■TRIM
・TRIM(文字列　from データ列名)
トリム方法に指定がないのでデフォルトの両端をトリムする。
■group by
している場合は、キーでグループ化しているので、一つとは限らない。
そのため単一行比較演算子が使えない。
条件を日本語にしてみて問題と一致しているか考える。
■テーブル定義
・デフォルト値にイコールは使えない。
・'SYSDATE'の場合
　'SYSDATE'を暗黙的変換　SYSDATE
 　SYSDATE→yyyy/mm/dd:Hi:mm:ss
   DATE型はデフォルト書式が「DD-MON-RR」となるため、SYSDATEと書式が一致しない。
   
