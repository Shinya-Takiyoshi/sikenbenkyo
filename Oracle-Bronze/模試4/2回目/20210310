■エスケープ文字
'%¥%_a%' escape '¥'の場合
%[%]_[a]% という文字として扱われる([]は文字列、それ以外はワイルドカード)
■SUM
・SUMは数値データ以外にも数値データ型に変換できる
・DISTINCTとUNIQUEは等価の扱い
■関数でネストしまくる問題
考え方
例)
 instr() どうなるか？
 substr() どうなるか？
 lpad() どうなるか？
 結果どうであるか？
 関数位で細分化して結果までどういう処理をしているかトレースする。
 
■日付書式の省略
デフォルト「DD-MON-RR」
MON→MONTH
RR→RRRR
に暗黙的に変換できる。

日付同士の比較
to_char(date,'rrrr')→4文字
to_date(date,'rr')→2文字
単位が一致していないため、比較ができずエラーになる。
