■interval[期間データ型]
省略した場合は、デフォルトの時間で計算される
例)25時間であれば
　+01 01:00:00(1日と1時間00分00秒)
■WIH TIES
問題文はcountで行を出していて、実際のSQL文は1行単位で問い合わせをしている。
offset 3 rows 7なので(3行スキップ、7行表示)
最後の行が3行あり、同じ値が続いているので、WITH TIESの場合追加で範囲とする。
